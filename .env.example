ENV=local
LOG_LEVEL=DEBUG

# Database
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=cc-job-ranking

# CORS (comma-separated origins)
CORS_ALLOWED_URL=http://localhost:3000

# Internal service auth
INTERNAL_TOKEN=changeme
